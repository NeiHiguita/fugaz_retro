@model IEnumerable<fugaz_retro.Models.Cliente>

@{
    ViewData["Title"] = "Clientes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="tbl">

    <h1>Lista de Clientes</h1>

    <div class="row mb-3">
@*         <div class="col">
            <a asp-action="Create" class="btn btn-success">Crear Nuevo <img src="/Images/agregar.png" height="24px" alt="Agregar" /></a>
        </div> *@
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-bordered" id="Table">
            <thead class="thead-light">
                <tr>
                    <th>Usuario</th>
                    <th>Teléfono</th>
@*                     <th>Acciones</th>
 *@                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Usuarios.NombreUsuario</td>
                        <td>@item.Telefono</td>
                     @*    <td>
                            <a asp-action="Edit" asp-route-id="@item.IdCliente"><i class="fas fa-pencil-alt mx-1" style="color: green; font-size: 20px"></i></a>
                            <a asp-action="Delete" asp-route-id="@item.IdCliente"><i class="fas fa-trash-alt mx-1" style="color: #bf0808; font-size: 20px"></i></a>
                        </td> *@
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
